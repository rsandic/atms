app.controller('MainController', ["$rootScope", "$scope", 'localStorageService',
    function($rootScope, $scope, localStorageService) {
        

        $scope.title = "ATMs"; 
        $scope.atmList = "null";  
        //planed to add user favorites location to local storage 
        
    }
]);